<resources>
    <!-- Default strings.xml. Specific localizations are in corresponding values-xx folders. -->
    <!-- Minimal content, e.g., just app_name if absolutely required by some tools, -->
    <!-- or can be left completely empty if the build system handles it. -->
    <!-- For PillPal, the main English strings are in values-en-rUS. -->
</resources>
